#!/bin/bash
echo 'Please insert the code from README.md and type in the sudo password'
cd /bin && sudo gedit simple-app
echo 'The App is being created, you should be able to type "simple-app" into your terminal'
